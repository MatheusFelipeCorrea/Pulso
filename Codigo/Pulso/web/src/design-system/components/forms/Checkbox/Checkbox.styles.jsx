import { cva } from 'class-variance-authority'

/** Layout only — cores/estados em base.css (.ds-checkbox-box) */
export const checkboxBoxVariants = cva('ds-checkbox-box')

export const checkboxIconVariants = cva('animate-[ds-check-pop_150ms_ease]')
